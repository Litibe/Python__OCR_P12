version: "3.9"

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - data_django_p12:/app
      - log_django_p12:/app/log
    ports:
      - 8000:8000

volumes:
  data_django_p12:
  log_django_p12:
